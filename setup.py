from setuptools import setup

import notify

setup(
    license=notify.__license__,
    description=notify.__description__,
    author=notify.__author__,
    author_email=notify.__author_email__,
    maintainer=notify.__author__,
    maintainer_email=notify.__author_email__,
    url=notify.__url__,
)
